{
  "name": "@felipeto/portfolio",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy:dev": "astro build && npx vercel",
    "deploy:prod": "astro build && npx vercel --prod"
  },
  "devDependencies": {
    "astro": "0.22.20"
  }
}